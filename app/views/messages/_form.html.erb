<%= form_with model: message, local: false do |f|%>
    <%= f.text_field :content%>
    <%= f.submit value %>
    <% if value=="更新" %>
      <%= link_to '取り消し', new_message_path, data:{remote: true} %>
    <% end %>
<%end%>